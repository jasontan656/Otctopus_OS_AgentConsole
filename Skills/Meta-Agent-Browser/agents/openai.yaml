interface:
  display_name: "Meta-Agent-Browser"
  short_description: "Manual-invoke skill for agent-browser plus browser fallback and Windows bridge entry docs"
  default_prompt: "Use Meta-Agent-Browser only for explicit browser automation. First run `python3 scripts/Cli_Toolbox.py contract --json`; then use `python3 scripts/Cli_Toolbox.py directive --topic runtime-entry --json`, `fallback-routing --json`, or `output-governance --json` by task intent. When files will be written, resolve governed paths with `python3 scripts/Cli_Toolbox.py paths --json` before using the skill-local wrappers."
