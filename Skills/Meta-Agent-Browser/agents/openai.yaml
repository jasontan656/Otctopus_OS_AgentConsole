interface:
  display_name: Meta-Agent-Browser
  short_description: Manual-invoke skill for agent-browser plus browser fallback and Windows bridge entry docs
  default_prompt: Use $Meta-Agent-Browser. First run `./.venv_backend_skills/bin/python Skills/Meta-Agent-Browser/scripts/Cli_Toolbox.py contract --json`. Treat CLI JSON as the primary runtime source and use markdown mirrors only when the contract still leaves a real gap.
