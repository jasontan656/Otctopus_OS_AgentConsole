interface:
  display_name: "Meta-Agent-Browser"
  short_description: "Manual-invoke skill for agent-browser plus browser fallback and Windows bridge entry docs"
  default_prompt: "Use Meta-Agent-Browser only when the user explicitly requests this skill or explicitly asks for browser automation; start from browser-total-entry when runtime selection is unclear."
