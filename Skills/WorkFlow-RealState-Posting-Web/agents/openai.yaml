interface:
  display_name: WorkFlow-RealState-Posting-Web
  short_description: 房源发布网页自动化 family/router skill，只在无头或有头两条分支之间路由。
  default_prompt: Use $WorkFlow-RealState-Posting-Web. First run `./.venv_backend_skills/bin/python Skills/WorkFlow-RealState-Posting-Web/scripts/Cli_Toolbox.py contract --json`. Treat CLI JSON as the primary runtime source and use markdown mirrors only when the contract still leaves a real gap.
