interface:
  display_name: Meta-github-operation
  short_description: 受限 GitHub control plane，仅服务 Octopus_OS 与 Otctopus_OS_AgentConsole，并对开发仓/发布仓分流、runtime claims 落盘与结果根目录施加硬约束。
  default_prompt: Use $Meta-github-operation. First run `./.venv_backend_skills/bin/python Skills/Meta-github-operation/scripts/Cli_Toolbox.py contract --json`. Treat CLI JSON as the primary runtime source and use markdown mirrors only when the contract still leaves a real gap.
