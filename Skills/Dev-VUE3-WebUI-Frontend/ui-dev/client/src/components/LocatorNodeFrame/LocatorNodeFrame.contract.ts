export const LOCATOR_NODE_FRAME_CONTRACT = {
  role: 'dev_support',
  variants: ['locator-on', 'locator-off'],
  description: 'Wraps a node and renders the visible locator chip when the global toggle is enabled.',
} as const
