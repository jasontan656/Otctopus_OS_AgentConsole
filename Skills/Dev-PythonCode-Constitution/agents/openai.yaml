interface:
  display_name: Dev-PythonCode-Constitution
  short_description: 关于 Python 代码规范与指引的开发技能。
  default_prompt: Use $Dev-PythonCode-Constitution. First run `./.venv_backend_skills/bin/python Skills/Dev-PythonCode-Constitution/scripts/Cli_Toolbox.py contract --json`. Treat CLI JSON as the primary runtime source and use markdown mirrors only when the contract still leaves a real gap.
