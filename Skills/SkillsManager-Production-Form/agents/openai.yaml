interface:
  display_name: SkillsManager-Production-Form
  short_description: 持续维护 console 目录的产品形态，并把 active 设计日志收敛到 governed runtime root。
  default_prompt: Use $SkillsManager-Production-Form. First run `./.venv_backend_skills/bin/python Skills/SkillsManager-Production-Form/scripts/Cli_Toolbox.py contract --json`. Treat CLI JSON as the primary runtime source and use markdown mirrors only when the contract still leaves a real gap.
