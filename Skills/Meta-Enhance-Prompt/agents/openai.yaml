interface:
  display_name: Meta-Enhance-Prompt
  short_description: Survey the current repo, supplement missing context, and emit a fixed-shape structured execution contract prompt.
  default_prompt: Use $Meta-Enhance-Prompt. First run `./.venv_backend_skills/bin/python Skills/Meta-Enhance-Prompt/scripts/Cli_Toolbox.py contract --json`. Treat CLI JSON as the primary runtime source and use markdown mirrors only when the contract still leaves a real gap.
