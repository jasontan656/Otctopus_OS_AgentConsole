# sync_mother_doc_navigation

## Purpose

- Refresh README.md and same-name scope markdown files under Mother_Doc only.
- Remove legacy docs-tree `AGENTS.md` files during navigation refresh.
- After navigation refresh, run the git-backed lifecycle sync so touched documents become `modified` and empty placeholders become `null`.
