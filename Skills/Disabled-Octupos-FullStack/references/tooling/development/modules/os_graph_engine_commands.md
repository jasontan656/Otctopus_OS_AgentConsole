# os_graph_engine_commands

## Purpose

- Run `analyze/query/context/impact/detect-changes/resource/map/wiki/cypher` inside the evidence-owned OS_graph engine while runtime assets stay inside `Mother_Doc/graph/runtime/`.
