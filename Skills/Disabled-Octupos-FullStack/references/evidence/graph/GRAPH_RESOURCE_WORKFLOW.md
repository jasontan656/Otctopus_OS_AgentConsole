# Graph Resource Workflow

适用阶段：`evidence > graph`

## Resource Outputs

- `maps/`: 结构地图、资源快照
- `wiki/`: 本地 graph-derived wiki
- `frontend_views/`: 前端聚合视图

## Rule

- `maps/` 与 `wiki/` 仍然属于机械运行产物。
- `frontend_views/` 是面向人类界面的聚合层，但来源仍是碎片化 graph runtime。
