# Mother_Doc Branch Note

- 当前 `mother_doc` 仍有三类任务：`direct_writeback`、`question_backfill`、`AGENTS manager`。
- AGENTS manager 当前只处理 `Octopus_OS/AGENTS.md`。
- AGENTS manager 的实际运行时入口固定走 branch contract、stage directive、target contract。
