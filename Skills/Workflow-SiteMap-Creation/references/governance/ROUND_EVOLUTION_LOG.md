# Round Evolution Log

本文件由 `self-governance-run` 按轮次追加，用于固化每次输入带来的规则变更、主链执行摘要与实验产物联动结果。

## 2026-03-17 :: mainline_reset
- change_theme: 从 `factory -> fixed artifact rewrite` 改造成 `factory -> intent enhance -> tmux subagent -> Functional-Analysis-Runtask -> artifact refresh -> validation closeout`
- runtime_enforcement:
  - 明确内置 `$Meta-Enhance-Prompt`
  - 明确内置 `$Functional-Analysis-Runtask analysis_loop`
  - 明确要求 `$Meta-keyword-first-edit`
  - 明确 tmux subagent 的 `gpt-5.4 / reasoning=high / idle=600s / manual terminate`
- artifact_expectation:
  - 样例产物必须展示字段语义、frontmatter、文档关系与消费边界
  - 产物刷新必须基于最新规则与证据对象，而不是固定模板重刷
