interface:
  display_name: SkillsManager-Naming-Manager
  short_description: 统一治理技能命名、注册、prefix/family 组织与集合调用语义。
  default_prompt: Use $SkillsManager-Naming-Manager. First run `./.venv_backend_skills/bin/python Skills/SkillsManager-Naming-Manager/scripts/Cli_Toolbox.py contract --json`. Treat CLI JSON as the primary runtime source and use markdown mirrors only when the contract still leaves a real gap.
