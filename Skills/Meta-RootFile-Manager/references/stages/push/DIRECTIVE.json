{
  "version": 2,
  "stage": "push",
  "tool_state": "active",
  "purpose": "Export the 治理映射模版 outward and overwrite the governed external targets.",
  "instruction": [
    "push exports only the rendered visible contract surface from the internal 治理映射模版 human source",
    "push must not export Part B to external AGENTS files",
    "push must strip frontmatter metadata and <part_A> wrappers from external AGENTS files",
    "push leaves internal Part B unchanged"
  ],
  "workflow": [
    "open the internal AGENTS 治理映射模版 human surface",
    "render the visible contract surface",
    "strip internal frontmatter metadata and <part_A> shell",
    "write only the rendered visible contract surface to the external AGENTS target"
  ],
  "rules": [
    "Part B is internal-only",
    "no separate machine sidecar is pushed externally",
    "push must use the 治理映射模版 human surface as the source of truth",
    "push must overwrite the governed external target directly"
  ]
}
