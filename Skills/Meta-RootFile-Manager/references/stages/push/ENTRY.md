---
doc_id: meta_rootfile_manager.references_stages_push_entry
doc_type: topic_atom
topic: Push Instruction
anchors:
- target: ../../../SKILL.md
  relation: implements
  direction: upstream
  reason: This document belongs to the governed skill tree under the main facade.
---

# Push Instruction

> Active stage semantics. Executable push tooling exists.

- Push only the rendered visible contract surface from the internal `治理映射模版` human source.
- Do not export `Part B`.
- Do not export frontmatter metadata or `<part_A>` wrappers.
- Keep internal `Part B` unchanged.
- Treat the internal `治理映射模版` as the source of truth for external overwrite.
