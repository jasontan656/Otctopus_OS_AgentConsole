---
owner: "由 `Otctopus_OS_AgentConsole` repository root container 所代表的 Python 测试运行配置面 负责；当前通过 `$Meta-RootFile-Manager` 的 `PYTEST_INI` 通道受管并同步。"
---
[pytest]
testpaths = Skills
norecursedirs = .git .venv_backend_skills frontend_skills node_modules
python_files = test_*.py
addopts = -ra --import-mode=importlib
