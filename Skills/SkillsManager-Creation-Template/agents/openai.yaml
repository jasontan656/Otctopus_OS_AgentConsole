interface:
  display_name: SkillsManager-Creation-Template
  short_description: 提供受治理技能模板与统一 Cli_Toolbox，用于创建或改造 basic 与 staged skill。
  default_prompt: Use $SkillsManager-Creation-Template. First run `./.venv_backend_skills/bin/python Skills/SkillsManager-Creation-Template/scripts/Cli_Toolbox.py contract --json`. Treat CLI JSON as the primary runtime source and use markdown mirrors only when the contract still leaves a real gap.
