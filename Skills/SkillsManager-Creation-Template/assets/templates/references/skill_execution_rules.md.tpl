---
doc_id: {{slug}}.references.policies.skill_execution_rules
doc_type: topic_atom
topic: Execution rules for {{skill_name}}
---

# Skill Execution Rules

- 文档是真源；CLI 只返回 contract 或动作结果。
- 保持 `metadata.skill_profile` 与目录实际形态一致。
- 需要改 tooling 时，同步更新 `references/tooling/` 与 `tests/`。
