# Execution_atom_plan&validation_packs Index

```python
# replace_me_fill_rule:
# - Replace every replace_me token before implementation starts.
# - This directory is the AI-facing construction-plan root. Use the filesystem slug `execution_atom_plan_validation_packs/` in paths and scripts.
# - Each numbered pack directory must stay independent so the AI can execute, validate, and backfill one pack at a time.
# - Do not collapse human-facing markdown, machine manifests, progress ledgers, and evidence registries into one file.
# - Remove this guidance block after drafting.
```

## 1. 根目录职责
- plan_root_role: replace_me
- designer_plan_source: replace_me
- pack_execution_rule: replace_me

## 2. pack 顺序
| pack_dir | design_step_id | pack_goal | machine_entry |
|---|---|---|---|
| `replace_me` | `replace_me` | `replace_me` | `replace_me` |

## 3. implementation 读取规则
- active_pack_only: replace_me
- pack_switch_rule: replace_me
- writeback_boundary: replace_me
