interface:
  display_name: "Meta-Skill-DocStructure"
  short_description: "规范 skill 内部文档结构与锚点图谱，内置 ui-dev 视图工具入口"
  default_prompt: "Use Meta-Skill-DocStructure only inside a skill folder. Enforce single-topic markdown atoms, frontmatter anchors, anchor graph JSON, and TypeScript CLI validation. Treat ui-dev as a separate embedded UI tool rather than part of the skill core."
