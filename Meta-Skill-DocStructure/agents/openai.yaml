interface:
  display_name: "Meta-Skill-DocStructure"
  short_description: "规范 skill 内部文档结构，并提供 TS CLI + Vue Flow 实时可视化门面"
  default_prompt: "Use Meta-Skill-DocStructure only inside a skill folder. Enforce single-topic markdown atoms, frontmatter anchors, anchor graph JSON, TypeScript CLI validation, and the Vue3 + Vue Flow live viewer/server workflow that defaults to SKILL.md."
