# mother_doc_agents_directive

## Purpose

- Emit the stage directive for `scan` / `collect` / `push` inside the Mother_Doc AGENTS/README manager branch.
- Use after the branch contract and before the concrete branch command.
