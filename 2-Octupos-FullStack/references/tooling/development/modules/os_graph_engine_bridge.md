# os_graph_engine_bridge

## Purpose

- Bridge `analyze/query/context/impact/detect-changes/resource/map/wiki/cypher` from the evidence graph subdomain into the current OS_graph engine source while runtime assets stay inside `Mother_Doc/graph/runtime/`.
