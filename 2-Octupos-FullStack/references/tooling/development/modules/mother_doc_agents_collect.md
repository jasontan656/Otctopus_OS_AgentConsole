# mother_doc_agents_collect

## Purpose

- Consume `scan_report.json` and pull current AGENTS files back into the skill-side registry.
- Refresh `assets/mother_doc_agents/index.md`, `registry.json`, and `collected_tree/`.
