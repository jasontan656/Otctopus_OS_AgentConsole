# SKILL Runtime Contract

- 运行时主源是 CLI JSON，而不是 markdown 审计文档。
- `mother_doc` 阶段中的 AGENTS 治理分支现在只治理一个外部目标：`Octopus_OS/AGENTS.md`。
- `Mother_Doc/docs` 树不再生成或维护递归 `AGENTS.md`。
- `mother-doc-agents-*` CLI 的职责是围绕 mirror 中的 managed human/machine pair 与外部根 `AGENTS.md` 做 scan / collect / push。
