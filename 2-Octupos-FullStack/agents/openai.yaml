interface:
  display_name: "2 Octupos Fullstack"
  short_description: "OctopusOS 全栈文档驱动开发与长期治理技能。"
  default_prompt: "固定按 `mother_doc -> construction_plan -> implementation -> acceptance` 推进；先读取 runtime contract 与 top-level rules，再进入当前阶段。把 mother_doc 作为长期需求与设计容器，把 fullstack graph 作为结构与合同补充，而不是需求源替代品。"
