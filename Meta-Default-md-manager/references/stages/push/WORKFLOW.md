# Push Workflow

1. Open the internal AGENTS human template.
2. Extract `<part_A> ... </part_A>`.
3. Write only that block to the external AGENTS target.
4. Leave the internal `Part B` and machine json untouched.
