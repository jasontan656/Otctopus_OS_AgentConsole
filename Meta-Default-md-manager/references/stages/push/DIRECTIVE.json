{
  "version": 2,
  "stage": "push",
  "tool_state": "active",
  "purpose": "Export managed internal templates outward and overwrite the governed external targets.",
  "instruction": [
    "push exports only Part A from the internal human template",
    "push must not export Part B to external AGENTS files",
    "push leaves internal Part B unchanged"
  ],
  "workflow": [
    "open the internal AGENTS human template",
    "extract the <part_A>...</part_A> block",
    "write only that block to the external AGENTS target"
  ],
  "rules": [
    "Part B is internal-only",
    "machine json is not pushed externally",
    "push must use the managed human template as the source of truth",
    "push must overwrite the governed external target directly"
  ]
}
