# Collect Workflow

1. Open the external managed AGENTS file.
2. Extract `<part_A> ... </part_A>`.
3. Create or refresh the matching internal managed directory and files.
4. Replace only the internal human template `Part A`.
5. Keep the internal `Part B` untouched.
6. Sync the same internal result into the installed skill directory.
