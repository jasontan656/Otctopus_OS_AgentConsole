{
  "version": 2,
  "stage": "collect",
  "tool_state": "inactive",
  "purpose": "Archive the intended collect semantics while no executable tooling exists.",
  "instruction": [
    "collect recovers only Part A from external AGENTS targets",
    "collect updates only the internal human template Part A",
    "collect must not overwrite the internal Part B"
  ],
  "workflow": [
    "open an external managed AGENTS file",
    "extract the <part_A>...</part_A> block",
    "replace only the internal human template Part A block",
    "leave Part B unchanged"
  ],
  "rules": [
    "machine json is not collected from external files",
    "Part B stays skill-internal during collect",
    "do not invent an executable collect command during the no-tool phase"
  ]
}
